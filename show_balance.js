const fs=require('fs');const s=fs.readFileSync('C:/Users/DELL/kire/frontend/src/App.tsx','utf8');const lines=s.split('\n');let bal=0;const out=[];for(let i=0;i<lines.length;i++){const l=lines[i];for(let c of l){ if(c=='{') bal++; else if(c=='}') bal--; } out.push({line:i+1,bal,text:l}); } const tail=out.slice(-200);console.log(tail.map(o=> (o.line.toString().padStart(4)+' '+o.bal.toString().padStart(4)+' | '+o.text)).join('\n'));
